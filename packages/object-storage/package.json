{
  "name": "@khoaluantn/object-storage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "r2": "dotenv -e ../../.env -- wrangler r2",
    "r2:buckets": "dotenv -e ../../.env -- wrangler r2 bucket list",
    "r2:ls": "dotenv -e ../../.env -- wrangler r2 object list courtin-images",
    "r2:cors:list": "dotenv -e ../../.env -- wrangler r2 bucket cors list courtin-images",
    "r2:cors:set": "dotenv -e ../../.env -- wrangler r2 bucket cors set courtin-images --file r2-cors.json -y"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.971.0",
    "@aws-sdk/s3-request-presigner": "^3.971.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
