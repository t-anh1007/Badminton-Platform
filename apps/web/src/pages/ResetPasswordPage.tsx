import { useState } from 'react';
import { Link, useSearchParams } from 'react-router-dom';
import { requestPasswordReset, resetPassword } from '../lib/accountApi';
import { Button, SurfaceCard, TextInput } from '../components/ui';
export function ResetPasswordPage() { const [params] = useSearchParams(); const token = params.get('token'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [confirm, setConfirm] = useState(''); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(''); setMessage(''); if (token && password !== confirm) { setError('Mật khẩu xác nhận chưa khớp.'); return; } setLoading(true); try { const result = token ? await resetPassword({ token, newPassword: password }) : await requestPasswordReset(email); setMessage(result.message); } catch (caught) { setError(caught instanceof Error ? caught.message : 'Không thể đặt lại mật khẩu.'); } finally { setLoading(false); } }; return <main className="min-h-[60vh] bg-canvas"><div className="page-container grid place-items-center py-12 sm:py-20"><SurfaceCard className="w-full max-w-md p-6 sm:p-8"><p className="courtin-kicker">COURTIN / TÀI KHOẢN</p><h1 className="mt-2 text-h1">{token ? 'Đặt mật khẩu mới' : 'Quên mật khẩu?'}</h1><p className="mt-3 text-sm leading-6 text-ink-500">{token ? 'Chọn mật khẩu mới có ít nhất 8 ký tự.' : 'Nhập email để nhận liên kết đặt lại mật khẩu.'}</p>{error && <p role="alert" className="mt-4 rounded-xl bg-danger-bg p-3 text-sm text-danger">{error}</p>}{message && <p role="status" className="mt-4 rounded-xl bg-success-bg p-3 text-sm text-success">{message}</p>}<form onSubmit={submit} className="mt-6 grid gap-4">{token ? <><label className="grid gap-1.5 text-sm font-semibold">Mật khẩu mới<TextInput type="password" required minLength={8} value={password} onChange={(event) => setPassword(event.target.value)} /></label><label className="grid gap-1.5 text-sm font-semibold">Xác nhận mật khẩu<TextInput type="password" required minLength={8} value={confirm} onChange={(event) => setConfirm(event.target.value)} /></label></> : <label className="grid gap-1.5 text-sm font-semibold">Email<TextInput type="email" required value={email} onChange={(event) => setEmail(event.target.value)} placeholder="ban@vidu.com" /></label>}<Button type="submit" className="w-full" disabled={loading}>{loading ? 'Đang gửi…' : token ? 'Lưu mật khẩu mới' : 'Gửi liên kết'}</Button></form><Link to="/auth" className="mt-5 inline-block text-sm font-bold text-brand-navy hover:underline">Quay lại đăng nhập</Link></SurfaceCard></div></main>; }
