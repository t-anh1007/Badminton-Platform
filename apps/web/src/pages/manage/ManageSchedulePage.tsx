import { useState } from 'react';import { useSearchParams } from 'react-router-dom';import { Button } from '../../components/ui';import { addClosure,saveOperatingHours } from '../../lib/venueBookingApi';const mins=(v:string)=>{const [h,m]=v.split(':').map(Number);return h*60+m};export function ManageSchedulePage(){const [q]=useSearchParams();const id=q.get('courtId')??'';const [open,setOpen]=useState('08:00'),[close,setClose]=useState('22:00'),[date,setDate]=useState(''),[error,setError]=useState('');return <section><h2 className="text-h2">Lịch hoạt động</h2><label>Giờ mở<input type="time" value={open} onChange={e=>setOpen(e.target.value)}/></label><label>Giờ đóng<input type="time" value={close} onChange={e=>setClose(e.target.value)}/></label><Button onClick={()=>{if(mins(open)>=mins(close))return setError('Giờ đóng phải sau giờ mở.');void saveOperatingHours(id,{weekday:1,openMinute:mins(open),closeMinute:mins(close)})}}>Lưu giờ hoạt động</Button><p role="alert">{error}</p><label>Ngày đóng (dd/MM/yyyy)<input placeholder="dd/MM/yyyy" value={date} onChange={e=>setDate(e.target.value)}/></label><Button onClick={()=>{const m=/^(\d{2})\/(\d{2})\/(\d{4})$/.exec(date);if(!m)return setError('Ngày phải theo dd/MM/yyyy.');void addClosure(id,{date:`${m[3]}-${m[2]}-${m[1]}T00:00:00.000Z`})}}>Thêm ngày đóng</Button></section>}
