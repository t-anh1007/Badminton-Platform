import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { Button,EmptyState } from '../../components/ui'; import { createManagedVenue,getMyManagedVenues,type ManagedVenue } from '../../lib/venueBookingApi';
export function ManageVenuesPage(){const [venues,setVenues]=useState<ManagedVenue[]>([]);const [open,setOpen]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [form,setForm]=useState({name:'',address:'',lat:'',lng:''});const load=()=>getMyManagedVenues().then(setVenues).catch(e=>setError(e.message));useEffect(()=>{load()},[]);const submit=async()=>{if(busy)return;setBusy(true);try{await createManagedVenue({...form,lat:Number(form.lat),lng:Number(form.lng),amenities:[],images:[]});setOpen(false);load()}catch(e){setError(e instanceof Error?e.message:'Không thể lưu')}finally{setBusy(false)}};return <section><div className="flex justify-between"><h2 className="text-h2">Cơ sở</h2><Button onClick={()=>setOpen(true)}>Thêm sân kinh doanh</Button></div>{error&&<p role="alert">{error}</p>}{open&&<div className="mt-4 grid gap-2"><input aria-label="Tên cơ sở" placeholder="Tên cơ sở" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input aria-label="Địa chỉ" placeholder="Địa chỉ" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><input aria-label="Vĩ độ" value={form.lat} onChange={e=>setForm({...form,lat:e.target.value})}/><input aria-label="Kinh độ" value={form.lng} onChange={e=>setForm({...form,lng:e.target.value})}/><Button disabled={busy} onClick={()=>void submit()}>Lưu cơ sở</Button></div>}{venues.length===0?<EmptyState title="Chưa có cơ sở" description="Bắt đầu thêm sân kinh doanh." action={<Button onClick={()=>setOpen(true)}>Thêm sân kinh doanh</Button>}/>:<div className="mt-5 grid gap-3">{venues.map(v=><Link key={v.id} to={`/manage/venues/${v.id}`} className="rounded-xl border p-4"><strong>{v.name}</strong><p>{v.address} · {v.courts.length} sân con</p></Link>)}</div>}</section>}
