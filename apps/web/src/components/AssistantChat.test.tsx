import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { it, expect } from 'vitest'; import { AssistantChat } from './AssistantChat.js'; it('renders two assistant columns', () => { render(<MemoryRouter><AssistantChat /></MemoryRouter>); expect(screen.getByText('Gợi ý kèo')).toBeInTheDocument(); expect(screen.getByText('Trò chuyện')).toBeInTheDocument() })
