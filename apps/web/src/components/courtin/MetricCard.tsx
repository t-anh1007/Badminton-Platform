import type { ReactNode } from 'react';
export function MetricCard({ label, value, detail, tone = 'default' }: { label: string; value: ReactNode; detail?: ReactNode; tone?: 'default' | 'navy' | 'yellow' }) { const tones = { default: 'bg-surface text-ink-900', navy: 'bg-brand-navy text-surface', yellow: 'bg-brand-yellow text-brand-navy' }; return <section className={`rounded-2xl border border-line p-5 shadow-[var(--shadow-card)] ${tones[tone]}`}><p className="text-caption opacity-75">{label}</p><p className="text-figures mt-2 text-2xl font-medium">{value}</p>{detail && <div className="mt-2 text-sm opacity-75">{detail}</div>}</section>; }
