import type { ReactNode } from 'react';
export function OperationsTable({ caption, columns, children }: { caption: string; columns: readonly string[]; children: ReactNode }) { return <div className="overflow-x-auto rounded-2xl border border-line bg-surface shadow-[var(--shadow-card)]"><table className="w-full min-w-[42rem] text-left text-sm"><caption className="sr-only">{caption}</caption><thead className="bg-canvas text-caption"><tr>{columns.map((column) => <th key={column} scope="col" className="px-5 py-3 font-bold">{column}</th>)}</tr></thead><tbody className="divide-y divide-line">{children}</tbody></table></div>; }
