import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { vi, it, expect } from 'vitest'; vi.mock('../lib/assistantApi.js', () => ({ getAssistantSession: () => ({ userId:'u', roles:['player'] }) })); import { AssistantBubble } from './AssistantBubble.js'; it('opens for an authenticated player', () => { render(<MemoryRouter><AssistantBubble /></MemoryRouter>); expect(screen.getByLabelText('Mở trợ lý AI')).toBeInTheDocument() })
