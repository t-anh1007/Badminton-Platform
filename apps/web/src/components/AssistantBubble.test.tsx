import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { vi, it, expect } from 'vitest'; vi.mock('../session/SessionProvider.js', () => ({ useSession: () => ({ session: { activeRole:'player' } }) })); import { AssistantBubble } from './AssistantBubble.js'; it('opens for an authenticated player', () => { render(<MemoryRouter><AssistantBubble /></MemoryRouter>); expect(screen.getByLabelText('Mở trợ lý AI')).toBeInTheDocument() })
