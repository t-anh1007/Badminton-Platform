import { useEffect, useState } from 'react';
export function Preloader() { const [visible, setVisible] = useState(true); const [fading, setFading] = useState(false); useEffect(() => { const hide = () => { setFading(true); window.setTimeout(() => setVisible(false), 300); }; if (document.readyState === 'complete') { const timer = window.setTimeout(hide, 250); return () => window.clearTimeout(timer); } window.addEventListener('load', hide); return () => window.removeEventListener('load', hide); }, []); if (!visible) return null; return <div className={`fixed inset-0 z-[9999] flex flex-col items-center justify-center bg-brand-navy transition-opacity duration-300 ${fading ? 'opacity-0' : 'opacity-100'}`} role="status" aria-label="Đang tải"><span aria-hidden className="grid h-14 w-14 animate-courtin-arrival place-items-center rounded-2xl bg-brand-yellow font-display text-3xl font-extrabold text-brand-navy">×</span><p className="mt-4 text-xs font-bold uppercase tracking-[0.1em] text-brand-yellow">Đang chuẩn bị sân cho bạn</p></div>; }
