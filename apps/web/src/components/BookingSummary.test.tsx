import { render, screen } from '@testing-library/react'; import { expect, it } from 'vitest'; import { BookingSummary } from './BookingSummary.js'; it('shows deterministic booking details', () => { render(<BookingSummary venue="Sân A" court="Court 1" range={{ courtId:'c',date:'2026-08-15',startAt:'2026-08-15T08:00:00+07:00',endAt:'2026-08-15T10:00:00+07:00',slotCount:2,durationMinutes:120,totalPrice:'180000' }} />); expect(screen.getByText(/2 slot/)).toBeInTheDocument() })
